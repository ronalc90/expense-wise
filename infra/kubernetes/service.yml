apiVersion: v1
kind: Service
metadata:
  name: expensewise-service
  namespace: expensewise
  labels:
    app.kubernetes.io/name: expensewise
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: expensewise
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
